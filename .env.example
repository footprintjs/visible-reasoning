# Copy this file to .env and paste your real Anthropic API key.
# Used only by the optional live mode:  npm run live  (about two small Haiku calls).
# The mock examples (npm run all) need no key. .env is git-ignored — never commit it.
ANTHROPIC_API_KEY=sk-ant-your-key-here
